<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{user.username}}!</h2>
  </div>
</div>

<div class="row mt-4 edit-post-container">
  <div class="col-md-6">
    <h3>Update Post:</h3>

    <form class="form edit-post-form">
      <div class="form-group">
        {{!-- <label for="post-id">{{post.id}}</label>
         <label for="post-user-id">{{post.user.id}}</label> --}}
         <label for="post_title">Title</label>
        <input class="form-input" type="text" id="editTitle" name="post_title" value="{{post.title}}"/>
      </div>
      <div class="form-group">
        <label for="commnent">Comment:</label>
        <input class="form-input" id="editComment" name="contents" value="{{post.contents}}"></input>
      </div>
      <div class="form-group">
        <button type="submit" value='Submit' class="btn btn-primary" id="editBtn">Submit</button>
      </div>


{{!-- You can coppy the delete button from the dashboard
delete button in the create a partial for delete button. --}}
      
<script src="/js/postedit.js"></script>