<div class="text-center">
  <h2>{{title}}</h2>

  <p>{{contents}}</p>
  <p>Created by {{user.username}} on {{format_date date_created}}</p>
  
  {{!-- #each comments here --}}
  <div class="justify-content-center">
  <p>
  {{#each comments}}
  <h4> {{comment}} </h4>
  <h4> Created by {{user.username}}</h4>
  <h5>{{date_created}}</h5>
  {{/each}}
 </p>
 </div>

  {{!-- If user is logged in, comment form --- adding post comment Api
  add event lister instert into the data base  --}}
<h3>Leave a comment:</h3>
<form class="form comment-form" id="comment-form" data-id="1">
<div class="form-group">
<input class="form-input" id="comment"></input>
</div>
<div class="form-group">
<button type="submit" value='Submit' class="btn btn-primary" id="createBtn">Create</button>
</div>
</div>
</form>




{{!-- <script src="./js/post.js"></script> --}}
<script src="/js/newcomment.js"></script>
