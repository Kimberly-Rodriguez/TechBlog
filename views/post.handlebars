<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4 edit-post-container">
  <div class="col-md-6">
    <h3>Update Post:</h3>

    <form class="form edit-post-form">
      <div class="form-group">
        <label for="post-title">Post Title:</label>
        <input class="form-input" type="text" id="post_title" name="post_title" />
      </div>
      <div class="form-group">
        <label for="contents">Contents:</label>
        <input class="form-input" id="contents" name="contents"></input>
      </div>
      <div class="form-group">
        <button type="submit" value='Submit' class="btn btn-primary">Create</button>
      </div>

  {{!-- {{#if posts.length}}
  <div class="col-md-6 post-list">
    <h3>Current Post:</h3> --}}

    {{#each posts as |post|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/post/{{post.id}}">{{post.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{post.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{!-- {{/if}} --}}
</div>
    </form>
  </div>

  {{!-- {{#if posts.length}}
  <div class="col-md-6 post-list">
    <h3>Current Post:</h3>

    {{#each posts as |post|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/post/{{post.id}}">{{post.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{post.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div> --}}

<script src="./js/update-post.js"></script>
